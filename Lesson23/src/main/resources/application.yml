spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:h2;mode=PostgreSQL
    username: sa
    password: sa
    initialization-mode: always
  jpa:
    hibernate:
      ddl-auto: none
  data:
    mongodb:
      database: lesson23

file:
  path:
    path: target/migration/
    books: target/migration/books.json
    authors: target/migration/authors.json
    genres: target/migration/genres.json

chunk:
  size: 5
